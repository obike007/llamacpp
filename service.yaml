apiVersion: v1
kind: Service
metadata:
  name: llamacpp-service
  namespace: llama-app
  labels:
    app: llamacpp
spec:
  type: NodePort
  ports:
  - port: 8083
    targetPort: 8083
    nodePort: 30083
    name: http
  selector:
    app: llamacpp